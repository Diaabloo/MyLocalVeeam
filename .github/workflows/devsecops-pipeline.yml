# name: MyLocalVeeam DevSecOps Pipeline

# on:
#   push:
#     branches: [ main, dev ]
#   pull_request:
#     branches: [ main ]

# jobs:
#   static-analysis:
#     name: Lint & SAST Security Scanning
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       # 1. Empêcher la fuite de secrets
#       - name: Secret Detection Scan
#         uses: trufflesecurity/trufflehog@main
#         with:
#           path: ./
#           base: ""

#       # 2. Scan de sécurité du code Terraform
#       - name: Terraform Security Scan (tfsec)
#         uses: aquasecurity/tfsec-pr-commenter-action@v1.2.0
#         with:
#           github_token: ${{ secrets.GITHUB_TOKEN }}

#   build-and-scan:
#     name: Secure Docker Build & Scan
#     needs: static-analysis
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       # 3. Build de l'image Docker
#       - name: Build Local Veeam Application Image
#         uses: docker/build-push-action@v4
#         with:
#           context: .
#           file: docker/app.Dockerfile
#           push: false
#           tags: mylocalveeam-api:latest

#       # 4. Scan de vulnérabilités sur ton image d'application
#       - name: Vulnerability Scan with Trivy
#         uses: aquasecurity/trivy-action@master
#         with:
#           image-ref: 'mylocalveeam-api:latest'
#           format: 'table'
#           exit-code: '1' # Bloque la pipeline s'il y a des failles critiques (Critical/High)
#           ignore-unfixed: true
#           vuln-type: 'os,library'
#           severity: 'CRITICAL,HIGH'