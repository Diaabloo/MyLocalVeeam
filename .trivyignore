# Ignore les vulnérabilités trouvées dans le binaire pré-compilé du client MinIO.
# C'est un choix pragmatique pour débloquer la pipeline, car nous ne contrôlons pas ses dépendances.
# Le risque est considéré comme faible car il s'agit d'un outil client utilisé pour des commandes spécifiques.
/usr/local/bin/mc